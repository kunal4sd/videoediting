<div
    name="{% if not content %}global_template_alert_playlist{% endif %}"
    class="list-group-item list-group-item-action list-group-item-success cursor-pointer"
    data-poster="{% if playlist.poster %}{{ playlist.poster }}{% endif %}"
    data-url="{% if playlist.url %}{{ playlist.url }}{% endif %}"
    data-publication="{% if playlist.publication %}{{ playlist.publication }}{% endif %}"
    data-hash="{% if playlist.hash %}{{ playlist.hash }}{% endif %}"
    data-url-texts="{% if playlist.url_texts %}{{ playlist.url_texts }}{% endif %}"
    data-url-vtt="{{ base_url() }}{{ path_for('video.view.vtt', { 'publication': 'publication', 'interval': 'interval', 'hash': 'hash' }) }}"
    data-url-files="{{ base_url() }}{{ path_for('video.view.files', { 'hash': 'hash' }) }}"
>
    <div class="d-fill row">
        <div class="col-sm-3 d-fill m-auto">
            {% if playlist.poster %}
                <img name="poster" src="{{ playlist.poster }}" class="rounded img-fluid">
            {% elseif playlist.url %}
                <img name="poster" src="" class="rounded img-fluid">
                <span name="no_poster" class="oi oi-audio"></span>
            {% else %}
                <img name="poster" src="" class="rounded img-fluid d-none">
                <span name="no_poster" class="oi oi-audio d-none"></span>
            {% endif %}
        </div>
        <div class="col-sm-9 d-fill m-auto row">
            <div class="text-left" name="start_day"> {% if playlist.start_day %}{{ playlist.start_day }}{% endif %}</div>&nbsp;&nbsp;
            <div class="text-left" name="start_hour">{% if playlist.start_hour %}{{ playlist.start_hour }}{% endif %} </div>
            <div class="text-left">&nbsp;-&nbsp;</div>
            <div class="text-left" name="end_hour"> {% if playlist.end_hour %}{{ playlist.end_hour }}{% endif %}</div>
        </div>
    </div>
</div>
